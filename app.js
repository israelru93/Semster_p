
this.on('conect',this.this.connecctionHandler);// חיבור לפונקציה שמאזינה לשרת שנימצא בקובץ SERVER.JS
const exspress=require('express');
const app=exspress();
const morgan = require('morgan');
app.use(morgan('common'));

Y
